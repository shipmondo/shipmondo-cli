[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shipmondo-cli"
version = "1.0.0"
dependencies = [
    "typer[all]",
    "requests"
]

[project.scripts]
shipmondo = "shipmondo.main:app"